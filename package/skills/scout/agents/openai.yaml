interface:
  display_name: "Scout"
  short_description: "Bounded docs and codebase research"
  default_prompt: "Use Scout to research a bounded docs/code question with qmd and codebase-memory, then report evidence, drift, and next workflow routes."

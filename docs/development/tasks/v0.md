# v0 Task Details (Active)

**Updated**: 2026-02-24
**Scope**: Non-complete v0 task details.

---

### T-097: Fix Gemini install — strip_frontmatter bug, TOML parse failures, cross-runtime path conflicts
**Updated**: 2026-02-24 — see `reports/analysis/2026-02-22-gemini-install-failure.md`
**AC**:
- `strip_frontmatter` fixed to stop after the second `---` delimiter only; subsequent `---` horizontal rules in SKILL.md bodies are not removed
- `skill_to_gemini_toml` switched to TOML literal multiline strings (`'''`) to eliminate backslash escape failures
- `FileCommandLoader` validation errors resolved: generated `.toml` files pass Gemini CLI validation for all skills
- Cross-runtime path conflict addressed: installer does not write Gemini skills to both `.gemini/skills/` and `.agents/skills/` simultaneously; Gemini CLI emits no `Skill conflict detected` warnings after install
- Commands mode: all orchestrator skills are available as Gemini commands without parse errors
- Skills mode: no duplicate-skill conflict warnings on Gemini CLI startup
- Conformance tests assert correct paths and no cross-runtime artifact duplication for Gemini
- `--check` output and runtime matrix documentation remain consistent with implemented Gemini behavior

---

## Notes

- T-100 exists in the backlog index but has no task-detail block in historical v0 backlog records.

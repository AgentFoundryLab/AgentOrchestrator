# v1 Task Details

**Updated**: 2026-02-25
**Scope**: v1 milestone — full Orchestrator vision: Strands framework, A2A protocol, observability, execution engine, advanced workflows, policy engine, cloud runtime.
**Prerequisite**: v0.3 complete (T-120 green)

---

## Task ID Range: T-121..T-180 (60 tasks)

| Phase | Epics | Tasks |
|-------|-------|-------|
| POC | Setup, Strands Framework Integration, Structured Memory | T-121..T-130 (10) |
| MVP | Multi-Provider Support, Workflow Refinement, Observability Foundation | T-131..T-139 (9) |
| Foundation | Evaluations, Data Foundation, Advanced Workflow Engine, Policy Engine | T-140..T-156 (17) |
| Factory | Cloud Runtime, Build Pipeline, Security Controls, OpenGuardrails, Resource Management, Autonomous Agents | T-157..T-180 (24) |
| **Total** | **16** | **60** |

---

## POC Phase

Strands Framework integration with A2A protocol foundation.

**Prerequisite**: v0.3 complete

---

### Epic: Setup

**Depends on**: v0.3 complete (installer redesign)

---

#### T-121: Build Orchestrator as globally installable Python binary
**Milestone**: v1
**Phase**: POC
**Epic**: Setup
**Depends on**: T-117 (Python package bootstrap)
**AC**:
- `uvx orchestrator` and `pipx install orchestrator` both work from PyPI / local build
- `orchestrator --version` prints semver
- Entry point registers as `orchestrator` in PATH after install
- Installation is idempotent; upgrade path documented

---

#### T-122: Devcontainer / Docker image with pre-installed dependencies
**Milestone**: v1
**Phase**: POC
**Epic**: Setup
**Depends on**: T-121
**AC**:
- `.devcontainer/devcontainer.json` present; opens in VS Code without manual setup
- `Dockerfile` builds a runnable image: `docker build -t orchestrator .`
- Image includes Python runtime, uvx/pipx, orchestrator package, all MCP deps
- Image < 1 GB compressed; layers cached on repeated builds

---

#### T-123: Global installation support (`orchestrator install`)
**Milestone**: v1
**Phase**: POC
**Epic**: Setup
**Depends on**: T-121, T-122
**Parallel with**: T-124
**AC**:
- `orchestrator install` installs runtime artifacts to `~/.orchestrator/` (global mode)
- `orchestrator install --project` installs to `.orchestrator/` (project mode)
- Idempotent; `orchestrator install --uninstall` reverses cleanly
- Help text documents all flags

---

### Epic: Strands Framework Integration

**Depends on**: T-121 (Setup)

---

#### T-124: Strands Framework setup
**Milestone**: v1
**Phase**: POC
**Epic**: Strands Framework Integration
**Depends on**: T-121
**AC**:
- `strands-agents` added to `pyproject.toml` dependencies
- Minimal Strands agent boots: `orchestrator run --agent swe` executes a no-op SWE agent
- Strands version pinned; upgrade path in `pyproject.toml` comments
- Unit test confirms Strands import resolves without error

---

#### T-125: Custom Anthropic provider with Claude SDK (not Client SDK)
**Milestone**: v1
**Phase**: POC
**Epic**: Strands Framework Integration
**Depends on**: T-124
**AC**:
- `orchestrator/providers/anthropic.py` wraps `anthropic` SDK (not `anthropic-client`)
- Provider passes Strands `model_provider` interface contract
- Integration test: Strands agent completes one tool-call round-trip via provider
- API key sourced from env `ANTHROPIC_API_KEY`; missing key raises `ConfigError` with actionable message

---

#### T-126: Orchestrator standard SWE Workflow implementation
**Milestone**: v1
**Phase**: POC
**Epic**: Strands Framework Integration
**Depends on**: T-125
**AC**:
- `orchestrator/workflows/swe.py` implements Spec→Design→Plan→Implement→Validate→Deploy→Document stages as Strands graph nodes
- Each stage is individually addressable: `orchestrator run --workflow swe --stage plan`
- `orchestrator run --workflow swe` executes full pipeline end-to-end on a test repo
- Existing skill SKILL.md files referenced as stage prompts (no duplication)

---

#### T-127: Git Worktree per Agent
**Milestone**: v1
**Phase**: POC
**Epic**: Strands Framework Integration
**Depends on**: T-126
**AC**:
- Each spawned agent gets a dedicated git worktree under `.orchestrator/worktrees/<agent-id>/`
- Worktree branches off current HEAD feature branch
- Agent merges its branch back to parent after self-validating acceptance criteria
- `--no-worktree` flag disables isolation for single-agent runs
- Worktrees cleaned up after successful merge or on `orchestrator clean`

---

### Epic: Structured Memory

**Depends on**: v0 Memory System

---

#### T-128: Well-defined file-based KB with TOC file and index refs
**Milestone**: v1
**Phase**: POC
**Epic**: Structured Memory
**Depends on**: T-121
**AC**:
- `docs/knowledge/` contains `TOC.md` listing all KB topics with one-line summaries and file paths
- Each KB file has frontmatter: `title`, `tags`, `updated`, `related` links
- `orchestrator kb index` regenerates `TOC.md` by scanning `docs/knowledge/`
- Stale `related` links detected and reported by `orchestrator kb lint`

---

#### T-129: Well-defined Serena memory structure with proper refs
**Milestone**: v1
**Phase**: POC
**Epic**: Structured Memory
**Depends on**: T-128
**Parallel with**: T-128
**AC**:
- `.serena/` directory has documented schema: `memory/`, `index/`, `sessions/`
- Each memory entry cross-references its source KB file via `source_doc` field
- `orchestrator memory sync` reconciles Serena index against `docs/knowledge/` TOC
- Orphaned Serena entries (no matching KB source) flagged by lint command

---

#### T-130: Context Manager agent
**Milestone**: v1
**Phase**: POC
**Epic**: Structured Memory
**Depends on**: T-128, T-129
**AC**:
- `orchestrator/agents/context_manager.py` Strands agent gathers high-signal data from session context
- Agent produces structured KB entry draft (frontmatter + content) for Orchestrator review
- `orchestrator context collect` runs context manager and outputs to `docs/knowledge/<topic>.md`
- Integration test: context manager processes a sample session log and produces valid KB frontmatter

---

## MVP Phase

A2A protocol, Strands Agents, HITL controls, and Observability.

**Prerequisite**: POC Phase complete (T-130)

---

### Epic: Multi-Provider Support

**Depends on**: T-124 (Strands Framework Integration)

---

#### T-131: A2A-MCP-Server for Gemini
**Milestone**: v1
**Phase**: MVP
**Epic**: Multi-Provider Support
**Depends on**: T-124
**Parallel with**: T-132
**AC**:
- `orchestrator/a2a/gemini.py` implements A2A server interface for Gemini CLI agent
- Agent card served at `http://localhost:<port>/.well-known/agent.json`
- Gemini agent receives task via A2A `tasks/send`; returns result via streaming response
- Integration test: Strands orchestrator delegates a task to Gemini A2A server and receives completion

---

#### T-132: A2A-MCP-Server for Codex
**Milestone**: v1
**Phase**: MVP
**Epic**: Multi-Provider Support
**Depends on**: T-124
**Parallel with**: T-131
**AC**:
- `orchestrator/a2a/codex.py` implements A2A server interface for Codex CLI agent
- Agent card served; Codex agent receives and responds to `tasks/send`
- `--role` flag selects Codex role config file for the spawned agent
- Integration test mirrors T-131 for Codex

---

#### T-133: Strands A2A Configuration and Discovery Setup
**Milestone**: v1
**Phase**: MVP
**Epic**: Multi-Provider Support
**Depends on**: T-131, T-132
**AC**:
- `orchestrator/a2a/registry.py` maintains runtime agent registry (name → A2A endpoint)
- `orchestrator agents list` shows registered agents and their A2A status
- Agent discovery supports both static config (`agents.yaml`) and dynamic registration
- Strands graph routes tasks to registered agents by capability tag

---

### Epic: Workflow Refinement

**Depends on**: T-126 (SWE Workflow)

---

#### T-134: Replace CC Hooks with Workflow steps
**Milestone**: v1
**Phase**: MVP
**Epic**: Workflow Refinement
**Depends on**: T-126
**AC**:
- `PreToolUse`, `PostToolUse`, `Stop` hook logic migrated to Strands workflow step nodes
- Claude Code hooks directory (`package/hooks/`) kept for backward compat but marked deprecated in docs
- Workflow steps fire in same relative order as replaced hooks
- E2E test: SWE workflow completes without relying on hook files

---

#### T-135: HITL controls integration
**Milestone**: v1
**Phase**: MVP
**Epic**: Workflow Refinement
**Depends on**: T-134
**AC**:
- HITL escalation point defined as first-class workflow node: `orchestrator/workflow/hitl.py`
- `orchestrator run --hitl` pauses workflow at HITL nodes and awaits stdin approval before continuing
- HITL protocol from `package/skills/hitl/SKILL.md` honoured: context, options, default action
- Integration test: workflow with HITL node pauses and resumes correctly on approved/rejected input

---

### Epic: Observability Foundation

**Depends on**: POC Phase complete

---

#### T-136: OTEL for Orchestrator
**Milestone**: v1
**Phase**: MVP
**Epic**: Observability Foundation
**Depends on**: T-124
**Parallel with**: T-131, T-132
**AC**:
- `opentelemetry-sdk` + `opentelemetry-exporter-otlp` in `pyproject.toml`
- `orchestrator run` emits root span `orchestrator.run` with attributes: workflow, stage, model, token_count
- OTLP exporter configurable via `OTEL_EXPORTER_OTLP_ENDPOINT` env var
- `--no-telemetry` flag disables all span emission

---

#### T-137: OTEL for Agents with parent span linkage
**Milestone**: v1
**Phase**: MVP
**Epic**: Observability Foundation
**Depends on**: T-136
**AC**:
- Each spawned agent receives parent trace context via W3C `traceparent` header
- Agent span is child of orchestrator root span; visible as nested in Jaeger/Tempo
- `agent.id`, `agent.runtime`, `agent.role` set as span attributes
- Integration test: two-agent run produces parent + two child spans with correct linkage

---

#### T-138: Prometheus + Loki integration
**Milestone**: v1
**Phase**: MVP
**Epic**: Observability Foundation
**Depends on**: T-137
**AC**:
- `/metrics` endpoint exposed (default `:9090/metrics`) with Prometheus text format
- Key metrics: `orchestrator_runs_total`, `orchestrator_tokens_total`, `orchestrator_agent_errors_total`
- Structured logs emitted in JSON to stdout; Loki-compatible labels: `workflow`, `stage`, `agent`
- `docker-compose.observability.yml` brings up Prometheus + Loki locally for development

---

#### T-139: Grafana Dashboards
**Milestone**: v1
**Phase**: MVP
**Epic**: Observability Foundation
**Depends on**: T-138
**AC**:
- `infra/grafana/dashboards/orchestrator.json` dashboard provisioned automatically on `docker compose up`
- Dashboard panels: run rate, token consumption, error rate, p50/p95 agent latency, active worktrees
- Dashboard imports cleanly into Grafana ≥ 10.x without manual JSON edits
- README documents how to access dashboards locally

---

## Foundation Phase

Ontology, Graph DB, Evaluations, Advanced Workflows, Policy Engine.

**Prerequisite**: MVP Phase complete (T-139)

---

### Epic: Evaluations

**Depends on**: T-136 (Observability Foundation)

---

#### T-140: Evaluation framework selection
**Milestone**: v1
**Phase**: Foundation
**Epic**: Evaluations
**Depends on**: T-139
**AC**:
- ADR documents evaluation framework decision: Langfuse vs Harbor vs OpenEvals
- Decision criteria: self-hostable, trace linkage with OTEL, open-source, Python SDK
- Selected framework added to `pyproject.toml` optional deps group `[eval]`
- Spike proof-of-concept logs one Strands agent run to selected framework

---

#### T-141: Evaluation framework integration
**Milestone**: v1
**Phase**: Foundation
**Epic**: Evaluations
**Depends on**: T-140
**AC**:
- `orchestrator/eval/client.py` wraps selected framework SDK
- Each SWE workflow run creates an eval trace with stage-level scores
- `orchestrator eval run <dataset>` executes a benchmark dataset and exports scores
- OTEL spans correlated with eval traces via shared `run_id`

---

#### T-142: Automated quality assessment
**Milestone**: v1
**Phase**: Foundation
**Epic**: Evaluations
**Depends on**: T-141
**AC**:
- Quality scorer evaluates: correctness (tests pass), completeness (AC coverage), hallucination rate
- Scores emitted as OTEL gauge metrics and stored in eval framework
- `orchestrator eval score --run-id <id>` prints per-stage breakdown
- Threshold config in `orchestrator.toml`: runs below threshold emit warning and exit non-zero

---

#### T-143: Output validation
**Milestone**: v1
**Phase**: Foundation
**Epic**: Evaluations
**Depends on**: T-141
**Parallel with**: T-142
**AC**:
- `orchestrator/eval/validators.py`: schema validator, syntax validator, diff-safety validator
- Validation runs automatically post-implement and post-deploy stages
- Validation failures block stage progression (unless `--skip-validation` passed with warning)
- Validators pluggable: custom validators registered via `orchestrator.toml [validators]`

---

### Epic: Data Foundation

**Depends on**: T-128 (Structured Memory)

---

#### T-144: Database selection
**Milestone**: v1
**Phase**: Foundation
**Epic**: Data Foundation
**Depends on**: T-130
**AC**:
- ADR documents DB selection across SQL/NoSQL/Graph/TS/OLAP dimensions
- Decision matrix covers: KB entities, agent telemetry, time-series metrics, analytics
- Selected stack: one primary operational DB + one OLAP (justified in ADR)
- Spike: selected DB spins up via `docker compose` and accepts a test write

---

#### T-145: Ontology schema
**Milestone**: v1
**Phase**: Foundation
**Epic**: Data Foundation
**Depends on**: T-144
**AC**:
- `docs/architecture/ontology.md` defines entity types: `Run`, `Agent`, `Task`, `Artifact`, `Decision`
- Relationships modelled: `Run → spawns → Agent`, `Agent → produces → Artifact`, `Task → blockedBy → Task`
- Schema versioned; migration path documented for schema changes
- JSON Schema files generated from ontology doc for runtime validation

---

#### T-146: Knowledge GraphDB integration
**Milestone**: v1
**Phase**: Foundation
**Epic**: Data Foundation
**Depends on**: T-145
**AC**:
- Graph DB (selected in T-144) stores entity/relationship graph per ontology schema
- `orchestrator kb graph --query <cypher/gremlin>` executes graph queries against KB
- Orchestrator writes `Task` nodes and `blockedBy` edges on each planning run
- Integration test: create 3 tasks with dependencies, query graph, verify edges correct

---

#### T-147: Time-series DB for prices and metrics
**Milestone**: v1
**Phase**: Foundation
**Epic**: Data Foundation
**Depends on**: T-144
**Parallel with**: T-146
**AC**:
- Time-series DB (e.g. InfluxDB, TimescaleDB, or VictoriaMetrics) receives OTEL metric export
- `orchestrator_tokens_total` and `orchestrator_agent_latency_seconds` stored with millisecond resolution
- Retention policy: 90 days default; configurable via `orchestrator.toml`
- Grafana data source wired to TS DB; latency panel in dashboard updated

---

#### T-148: Analytics OLAP
**Milestone**: v1
**Phase**: Foundation
**Epic**: Data Foundation
**Depends on**: T-147
**AC**:
- OLAP store (e.g. DuckDB or ClickHouse) receives nightly ETL from operational DB
- `orchestrator analytics export --format parquet` exports run history
- Pre-built queries: cost per workflow, error rate by agent, token efficiency trend
- Analytics results accessible via `orchestrator analytics query "<sql>"`

---

### Epic: Advanced Workflow Engine

**Depends on**: T-126 (Strands Agents)

---

#### T-149: L2 Graph — conditional routing via Strands Graph + A2A
**Milestone**: v1
**Phase**: Foundation
**Epic**: Advanced Workflow Engine
**Depends on**: T-133
**AC**:
- Strands Graph node supports conditional edge evaluation: `if result.confidence < 0.8: route → review`
- Routing conditions expressed in `orchestrator.toml` workflow DSL
- A2A `tasks/send` used when routing to cross-runtime agent
- Integration test: graph routes to review node on low-confidence output; skips review on high-confidence

---

#### T-150: L3 Swarm — autonomous mesh via Strands Swarm + A2A native
**Milestone**: v1
**Phase**: Foundation
**Epic**: Advanced Workflow Engine
**Depends on**: T-149
**AC**:
- Strands Swarm initialises multi-agent mesh from agent registry (T-133)
- Each swarm member operates autonomously; results aggregated by coordinator
- A2A native protocol used for inter-agent communication (no central message broker)
- `orchestrator swarm run --workflow swe --agents 3` completes a parallelised SWE task

---

#### T-151: Result aggregation
**Milestone**: v1
**Phase**: Foundation
**Epic**: Advanced Workflow Engine
**Depends on**: T-150
**Parallel with**: T-152
**AC**:
- `orchestrator/workflow/aggregator.py` merges multi-agent outputs: code patches, reports, plan diffs
- Merge strategy selectable: `first-wins`, `vote`, `union`, `llm-merge`
- Aggregation result stored as `Artifact` node in knowledge graph (T-146)
- Integration test: 3 agents produce overlapping patches; aggregator merges without conflicts

---

#### T-152: Conflict resolution
**Milestone**: v1
**Phase**: Foundation
**Epic**: Advanced Workflow Engine
**Depends on**: T-150
**Parallel with**: T-151
**AC**:
- Conflict detector identifies: overlapping file edits, contradictory plan steps, schema violations
- On conflict: HITL escalation node invoked (T-135) with diff and resolution options
- Automatic resolution attempted for non-overlapping conflicts (different file scopes)
- Conflict history logged to knowledge graph for future pattern detection

---

### Epic: Policy Engine

**Depends on**: T-145 (Data Foundation)

---

#### T-153: Steering contextual feedback
**Milestone**: v1
**Phase**: Foundation
**Epic**: Policy Engine
**Depends on**: T-145
**AC**:
- `orchestrator/policy/steering.py` injects contextual feedback into agent prompts based on active policies
- Feedback injected at workflow stage boundaries (pre-stage context enrichment)
- Steering rules defined in `docs/policy/STANDARDS.md` and loaded at runtime
- Unit test: given a policy rule, steering module produces correct prompt injection

---

#### T-154: Policy-as-Code (OPA) integration
**Milestone**: v1
**Phase**: Foundation
**Epic**: Policy Engine
**Depends on**: T-153
**AC**:
- OPA (Open Policy Agent) added as optional dependency; `orchestrator policy eval <input>` runs Rego policy
- Core policies migrated to `.rego` files under `orchestrator/policy/rego/`
- Policy decision logged as `PolicyDecision` entity in knowledge graph
- `orchestrator policy test` runs all `.rego` unit tests and reports pass/fail

---

#### T-155: Agents RBAC / permissions management
**Milestone**: v1
**Phase**: Foundation
**Epic**: Policy Engine
**Depends on**: T-154
**AC**:
- Role definitions in `orchestrator.toml [roles]`: `reader`, `planner`, `executor`, `admin`
- Each role has an allowed-tools list; agent receives only its allowed tools
- RBAC enforced at A2A task dispatch: unauthorized tool call returns `403` with policy reason
- Integration test: executor role cannot call `git push`; admin role can

---

#### T-156: Dynamic policy evaluation
**Milestone**: v1
**Phase**: Foundation
**Epic**: Policy Engine
**Depends on**: T-155
**AC**:
- Policy rules hot-reloaded from `orchestrator/policy/rego/` without restart
- `orchestrator policy reload` triggers OPA bundle refresh
- Dynamic context (current run metadata, token budget, time of day) available to Rego rules
- Policy evaluation latency < 10 ms p99 under load (measured by benchmark test)

---

## Factory Phase

Cloud Runtime, Build Pipeline, Security, Resource Management, Guardrails.

**Prerequisite**: Foundation Phase complete (T-156)

---

### Epic: Cloud Runtime

**Depends on**: T-122 (POC Devcontainer)

---

#### T-157: Docker in K8s / Cloud Run
**Milestone**: v1
**Phase**: Factory
**Epic**: Cloud Runtime
**Depends on**: T-122
**Parallel with**: T-158
**AC**:
- `infra/k8s/` contains Deployment, Service, and ConfigMap manifests for orchestrator
- `infra/cloudrun/` contains Cloud Run `service.yaml`
- `orchestrator` container starts cleanly on both targets: `kubectl apply` and `gcloud run deploy`
- Health check endpoint `GET /healthz` returns 200 within 5 s of cold start

---

#### T-158: Infra provisioning
**Milestone**: v1
**Phase**: Factory
**Epic**: Cloud Runtime
**Depends on**: T-157
**AC**:
- Terraform (or Pulumi) modules under `infra/terraform/` provision: VPC, GKE/CloudRun, artifact registry, IAM
- `terraform plan` produces no errors on a fresh GCP project
- All resources tagged with `managed-by = orchestrator`
- Destroy plan removes all managed resources without leaving orphans

---

#### T-159: Environment isolation
**Milestone**: v1
**Phase**: Factory
**Epic**: Cloud Runtime
**Depends on**: T-157
**AC**:
- Each workflow run gets an ephemeral container / namespace scoped to that run ID
- Run containers share no writable filesystem mounts with sibling runs
- `orchestrator run --isolated` enforces container-level isolation regardless of deployment target
- Isolation verified by test: concurrent runs writing to same path produce no cross-contamination

---

#### T-160: VPC / INET isolation
**Milestone**: v1
**Phase**: Factory
**Epic**: Cloud Runtime
**Depends on**: T-158
**Parallel with**: T-159
**AC**:
- Agent containers operate in a private VPC subnet with no direct internet egress by default
- Egress whitelist configurable via `orchestrator.toml [network.egress_allow]`
- DNS resolution restricted to internal resolver; external DNS blocked
- Network policy (`NetworkPolicy` in K8s or VPC firewall in GCP) enforced in CI smoke test

---

### Epic: Build Pipeline

**Depends on**: T-157 (Cloud Runtime)

---

#### T-161: GitOps integration
**Milestone**: v1
**Phase**: Factory
**Epic**: Build Pipeline
**Depends on**: T-157
**AC**:
- ArgoCD (or Flux) `Application` manifest in `infra/gitops/` syncs `infra/k8s/` on merge to `main`
- `orchestrator deploy` pushes image tag and opens a GitOps PR updating the manifest
- Sync status visible via `orchestrator deploy status`
- Rollback: `orchestrator deploy rollback` reverts image tag in manifest and forces sync

---

#### T-162: MLOps integration
**Milestone**: v1
**Phase**: Factory
**Epic**: Build Pipeline
**Depends on**: T-161
**Parallel with**: T-163
**AC**:
- Model registry (MLflow or Weights & Biases) stores prompt versions and eval score history
- `orchestrator mlops register --prompt <file>` registers a prompt version with current eval scores
- Promotion gate: model/prompt promoted to production only if eval score ≥ threshold (T-142)
- Eval lineage traceable: each production run links back to registered prompt version

---

#### T-163: CI/CD workflow definitions
**Milestone**: v1
**Phase**: Factory
**Epic**: Build Pipeline
**Depends on**: T-161
**Parallel with**: T-162
**AC**:
- `.github/workflows/ci.yml`: lint, test, build, push image, update GitOps manifest
- `.github/workflows/release.yml`: tag-triggered; runs full eval suite before publish
- All existing tests run in CI without manual intervention
- CI completes in < 10 min on GitHub Actions standard runner

---

#### T-164: Artifact registry management
**Milestone**: v1
**Phase**: Factory
**Epic**: Build Pipeline
**Depends on**: T-163
**AC**:
- Container images pushed to GCP Artifact Registry (or GHCR) on every CI green merge
- Image tags: `latest`, `<semver>`, `<git-sha>`
- Retention policy: keep last 10 semver tags; delete untagged digests older than 7 days
- `orchestrator artifacts list` prints available image tags with build metadata

---

### Epic: Security Controls

**Depends on**: T-157 (Cloud Runtime)

---

#### T-165: Secret Manager integration
**Milestone**: v1
**Phase**: Factory
**Epic**: Security Controls
**Depends on**: T-157
**AC**:
- GCP Secret Manager (or 1Password / KeePass via CLI) integration in `orchestrator/secrets.py`
- `orchestrator secrets get <name>` fetches secret and injects into env for current run
- Secrets never written to disk or logged; redacted in OTEL spans
- Integration test: mock secret manager returns value; verify env injection and no-log behavior

---

#### T-166: Agents OAuth and Authentication
**Milestone**: v1
**Phase**: Factory
**Epic**: Security Controls
**Depends on**: T-165
**AC**:
- Each agent identity has an OAuth 2.0 client credential (subscription-based)
- A2A `tasks/send` requests signed with agent JWT; server validates before processing
- Token refresh handled transparently; expired tokens do not break in-flight tasks
- `orchestrator auth login` and `orchestrator auth revoke <agent>` manage agent credentials

---

#### T-167: Cross-container restrictions
**Milestone**: v1
**Phase**: Factory
**Epic**: Security Controls
**Depends on**: T-165
**Parallel with**: T-166
**AC**:
- Container security context: `readOnlyRootFilesystem: true`, `allowPrivilegeEscalation: false`, non-root UID
- Inter-container communication restricted to A2A port only (no arbitrary socket access)
- `seccomp` profile applied; forbidden syscalls logged and blocked
- Policy scan (e.g. `kube-score` or `trivy`) passes on all manifests in CI

---

#### T-168: Runtime / CI / Cloud permissions escalation controls
**Milestone**: v1
**Phase**: Factory
**Epic**: Security Controls
**Depends on**: T-166
**AC**:
- IAM least-privilege enforced: each service account has only required roles
- `orchestrator policy audit` scans IAM bindings and reports over-privileged accounts
- CI service account cannot push to production; separate production SA gated on GitOps merge
- Audit log alerts configured for privilege escalation events in GCP

---

### Epic: OpenGuardrails

**Depends on**: T-165 (Security Controls)

---

#### T-169: Command restriction in "Bypass permissions" mode
**Milestone**: v1
**Phase**: Factory
**Epic**: OpenGuardrails
**Depends on**: T-165
**AC**:
- `orchestrator.toml [guardrails.bypass_mode.deny_commands]` list blocks specified shell commands even when bypass is active
- Blocked command attempt logged as `GuardrailViolation` event and surfaced in OTEL
- Integration test: `rm -rf /` blocked; safe commands pass
- Default deny list published in docs; users can extend, not shrink

---

#### T-170: Content filtering
**Milestone**: v1
**Phase**: Factory
**Epic**: OpenGuardrails
**Depends on**: T-169
**Parallel with**: T-171
**AC**:
- `orchestrator/guardrails/content_filter.py` scans LLM outputs for policy violations before tool execution
- Configurable rule sets: profanity, off-topic domain, code injection patterns
- Filtered output triggers HITL escalation (T-135) rather than silent drop
- Filter latency < 50 ms p99 on 4K token payload

---

#### T-171: PII / secrets redaction
**Milestone**: v1
**Phase**: Factory
**Epic**: OpenGuardrails
**Depends on**: T-169
**Parallel with**: T-170
**AC**:
- `orchestrator/guardrails/redactor.py` detects and redacts: API keys, passwords, emails, phone numbers, SSNs
- Redaction applied to: LLM inputs, outputs, OTEL span attributes, log lines
- Redacted tokens replaced with `[REDACTED:<type>]` placeholder
- Unit test: 10 sample PII patterns all redacted; non-PII text passes unmodified

---

#### T-172: Policy enforcement hooks
**Milestone**: v1
**Phase**: Factory
**Epic**: OpenGuardrails
**Depends on**: T-170, T-171
**AC**:
- `orchestrator/guardrails/hooks.py` fires before and after each tool call: `pre_tool`, `post_tool`
- Hooks invoke OPA policy (T-154) and content filter (T-170) in sequence
- Hook result: `allow`, `deny`, `escalate`; deny returns structured error to agent
- Hook pipeline configurable in `orchestrator.toml [guardrails.hooks]`

---

### Epic: Resource Management

**Depends on**: T-136 (Observability Foundation)

---

#### T-173: Token budget allocation
**Milestone**: v1
**Phase**: Factory
**Epic**: Resource Management
**Depends on**: T-136
**AC**:
- `orchestrator.toml [resources.token_budget]` per-run and per-agent limits
- Budget tracker deducts tokens from OTEL span attributes after each LLM call
- Near-limit warning at 80%: agent receives system message "budget low, be concise"
- Budget exhaustion: run halted gracefully; partial results saved; exit code 2

---

#### T-174: Compute resource limits
**Milestone**: v1
**Phase**: Factory
**Epic**: Resource Management
**Depends on**: T-157
**Parallel with**: T-173
**AC**:
- K8s `resources.requests` and `resources.limits` set on all containers (CPU + memory)
- Default limits: 500m CPU / 512 Mi memory per agent container
- Limits configurable via `orchestrator.toml [resources.compute]`
- OOM-killed container triggers run failure with structured error (not silent hang)

---

#### T-175: Cost tracking
**Milestone**: v1
**Phase**: Factory
**Epic**: Resource Management
**Depends on**: T-173, T-174
**AC**:
- `orchestrator_cost_usd_total` Prometheus counter emitted per run with model, workflow, agent labels
- `orchestrator cost report --since 7d` prints cost breakdown by workflow and agent
- Cost thresholds in `orchestrator.toml`; run aborted if projected cost exceeds limit
- Cost data stored in OLAP (T-148) for historical trend queries

---

#### T-176: Circuit breakers and rate limiting
**Milestone**: v1
**Phase**: Factory
**Epic**: Resource Management
**Depends on**: T-175
**AC**:
- Circuit breaker wraps each external API call (Anthropic, Gemini, Codex, GCP)
- States: closed → open (after 5 consecutive failures) → half-open (after 30 s)
- Rate limiter: configurable RPS cap per API; excess requests queued up to `queue_depth` then rejected
- `orchestrator_circuit_breaker_state` gauge metric emitted per API; Grafana panel added

---

### Epic: Autonomous Agents

**Depends on**: T-150 (Advanced Workflow Engine), T-169 (OpenGuardrails)

---

#### T-177: Sensor / Ambient agent framework
**Milestone**: v1
**Phase**: Factory
**Epic**: Autonomous Agents
**Depends on**: T-150, T-169
**AC**:
- `orchestrator/agents/sensor.py` base class for long-running ambient agents
- Sensor agents subscribe to event streams (git webhooks, file watchers, metrics thresholds)
- `orchestrator sensor start <name>` daemonises a sensor agent; `orchestrator sensor stop <name>` terminates cleanly
- Sensor lifecycle (start, error, stop) logged to knowledge graph

---

#### T-178: Background monitoring routines
**Milestone**: v1
**Phase**: Factory
**Epic**: Autonomous Agents
**Depends on**: T-177
**Parallel with**: T-179
**AC**:
- Built-in monitors: drift detector (file changes vs last commit), token budget sentinel, eval score regression alert
- Each monitor runs on configurable cron schedule via `orchestrator.toml [monitors]`
- Monitor alerts routed to HITL escalation (T-135) or notification channel (Slack webhook / email)
- `orchestrator monitor status` lists active monitors with last-run timestamp and alert count

---

#### T-179: Goal-oriented research agents
**Milestone**: v1
**Phase**: Factory
**Epic**: Autonomous Agents
**Depends on**: T-177
**Parallel with**: T-178
**AC**:
- `orchestrator/agents/researcher.py` Strands agent: accepts a research goal, decomposes to sub-queries, executes via MCP search tools, returns structured KB entry
- `orchestrator research "<goal>"` runs researcher agent and writes result to `docs/knowledge/`
- Research results stored as `Artifact` in knowledge graph with source citations
- HITL gate before KB write: user reviews and approves/edits draft entry

---

#### T-180: Proactive collaboration
**Milestone**: v1
**Phase**: Factory
**Epic**: Autonomous Agents
**Depends on**: T-178, T-179
**AC**:
- Proactive agent monitors knowledge graph for stale decisions (ADR older than 90 days with no review)
- Surfaces suggestions to user via HITL: "ADR-003 may be outdated — review?"
- Collaboration trigger configurable: staleness threshold, coverage gaps, eval regressions
- `orchestrator collaborate --dry-run` shows what suggestions would be raised without sending them

---

## Parallelization Map

```
POC:
  T-121 (binary) → T-122 (devcontainer) → T-123 (install)
  T-121 → T-124 (strands) → T-125 (provider) → T-126 (workflow) → T-127 (worktree)
  T-121 → T-128 (KB) ∥ T-129 (serena) → T-130 (context mgr)

MVP:
  T-131 (gemini A2A) ∥ T-132 (codex A2A) → T-133 (discovery)
  T-126 → T-134 (hooks→steps) → T-135 (HITL)
  T-124 → T-136 (otel) → T-137 (agent spans) → T-138 (prom+loki) → T-139 (grafana)

Foundation:
  T-139 → T-140 → T-141 → T-142 ∥ T-143
  T-130 → T-144 → T-145 → T-146 ∥ T-147 → T-148
  T-133 → T-149 → T-150 → T-151 ∥ T-152
  T-145 → T-153 → T-154 → T-155 → T-156

Factory:
  T-122 → T-157 ∥ T-158 → T-159 ∥ T-160
  T-157 → T-161 → T-162 ∥ T-163 → T-164
  T-157 → T-165 → T-166 ∥ T-167 → T-168
  T-165 → T-169 → T-170 ∥ T-171 → T-172
  T-136 → T-173 ∥ T-174 → T-175 → T-176
  T-150+T-169 → T-177 → T-178 ∥ T-179 → T-180
```
